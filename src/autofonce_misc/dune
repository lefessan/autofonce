; generated by drom from package skeleton 'library'

(library
  (name autofonce_misc)
  (public_name autofonce_misc)
  (wrapped true)
  (libraries ocplib_stuff ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_misc))


