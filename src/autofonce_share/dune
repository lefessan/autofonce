; generated by drom from package skeleton 'library'

(library
  (name autofonce_share)
  (public_name autofonce_share)
  (wrapped true)
  (libraries  bigarray)
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_share))

(rule
 (target tree.ml)
 (deps
  (source_tree files))
 (action
  (with-stdout-to
   %{null}
   (run ocaml-crunch -m plain files -o %{target}))))

