; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name autofonce)
  (package autofonce)
  (libraries autofonce_lib )
  
  (flags (:standard (:include linking.sexp)))
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce))

(rule
  (targets linking.sexp)
  (action  (with-stdout-to %{targets}
    (run ./linking_flags.sh linking.sexp %{env:LINKING_MODE=dynamic} %{ocaml-config:system}))))


