; generated by drom from package skeleton 'library'

(library
  (name autofonce_lib)
  (public_name autofonce_lib)
  (wrapped true)
  (libraries ocplib_stuff ez_file ez_cmdliner autofonce_share autofonce_patch autofonce_m4 autofonce_core autofonce_config ANSITerminal )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_lib))


