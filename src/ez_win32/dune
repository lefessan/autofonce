; generated by drom from package skeleton 'library'

(library
  (name ez_win32)
  (public_name ez_win32)
  (wrapped true)
  (libraries ez_file )
  
  (foreign_stubs (language c) (names onlyWin32_c win32Registry_c))
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ez_win32))


