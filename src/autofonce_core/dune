; generated by drom from package skeleton 'library'

(library
  (name autofonce_core)
  (public_name autofonce_core)
  (wrapped true)
  (libraries ocplib_stuff ez_file ez_cmdliner autofonce_share autofonce_m4 ANSITerminal )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_core))


