; generated by drom from package skeleton 'library'

(library
  (name autofonce_config)
  (public_name autofonce_config)
  (wrapped true)
  (libraries toml ocplib_stuff ez_file autofonce_misc )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_config))


