; generated by drom from package skeleton 'library'

(library
  (name autofonce_m4)
  (public_name autofonce_m4)
  (wrapped true)
  (libraries ocplib_stuff ez_file autofonce_misc )
  
  
  )

(ocamllex m4Lexer)

(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package autofonce_m4))


